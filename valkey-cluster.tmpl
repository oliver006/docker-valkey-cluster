bind ${BIND_ADDRESS}
port ${PORT}
cluster-enabled yes
cluster-config-file nodes.conf
cluster-node-timeout 15000
${CLUSTER_DATABASES_CONFIG}
appendonly yes
dir /valkey-data/${NODE_PORT}
protected-mode no
requirepass "${VALKEY_PASSWORD}"
masterauth "${VALKEY_PASSWORD}"
